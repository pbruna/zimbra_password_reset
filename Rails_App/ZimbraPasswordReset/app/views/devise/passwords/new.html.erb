<div id="loggin_box">
<h2>Olvidó su contraseña de correo?</h2>

<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, :required => true, :label => "Ingrese su email de Zimbra", :autofocus => true, :placeholder => "cuenta@ucsc.cl" %>
	<p>
		Ingresa tu dirección @ucsc.cl y enviaremos las instrucciones para actualizar la contraseña a tu casilla de correo personal.
	</p>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Enviar instrucciones", :class => "btn btn-success" %>
  </div>
<% end %>
</div>