## Authorizations
# Uncomment out the merging for each enviornment that you'd like to include.
# You can also just copy and paste the tree (do not include the "authorizations") to each
# enviornment if you need something different per enviornment.
#authorizations: &AUTHORIZATIONS
  # group_base: ou=people,
  #   ## Requires config.ldap_check_group_membership in devise.rb be true
  #   # Can have multiple values, must match all to be authorized
  #   required_groups:
  #     # If only a group name is given, membership will be checked against "uniqueMember"
  #     - cn=admins,ou=groups,dc=test,dc=com
  #     - cn=users,ou=groups,dc=test,dc=com
  #     # If an array is given, the first element will be the attribute to check against, the second the group name
  #     - ["moreMembers", "cn=users,ou=groups,dc=test,dc=com"]
  #   ## Requires config.ldap_check_attributes in devise.rb to be true
  #   ## Can have multiple attributes and values, must match all to be authorized
  #   require_attribute:
  #     objectClass: inetOrgPerson
  #     authorizationRole: postsAdmin

## Enviornments

development:
  host: 127.0.0.1
  port: 389
  attribute: zimbraMailDeliveryAddress
  base: ""
  require_attribute:
      zimbraisadminaccount: "FALSE"
  admin_user: uid=zimbra,cn=admins,cn=zimbra
  admin_password: KvhD6KXTf5
  url_for_not_admins: "http://www.google.cl/"
  ssl: false
  backend_url: "https://intranet.ucsc.cl/escritorio/modulos/funcionarios/consultas/multiconsultor.php"
  backend_key: "17c4a8d52981caed80d574fffa70c4e3"
  app_email_from: "soporte@ucsc.cl"
#  <<: *AUTHORIZATIONS

test:
  host: localhost
  port: 3389
  attribute: cn
  base: ou=people,dc=test,dc=com
  admin_user: cn=admin,dc=test,dc=com
  admin_password: admin_password
  ssl: simple_tls
  # <<: *AUTHORIZATIONS

production:
  host: localhost
  port: 636
  attribute: cn
  base: ou=people,dc=test,dc=com
  admin_user: cn=admin,dc=test,dc=com
  admin_password: admin_password
  ssl: start_tls
  # <<: *AUTHORIZATIONS
